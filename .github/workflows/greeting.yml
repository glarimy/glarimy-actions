name: Greeting with custom workflow
on:
  workflow_dispatch:
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: greets the person
        uses: glarimy/greeting-action@v1.1
        with:
          person-name: Krishna
